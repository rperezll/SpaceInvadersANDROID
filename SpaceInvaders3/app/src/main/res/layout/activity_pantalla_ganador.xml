<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_pantalla_ganador"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.winrob.spaceinvaders3.pantalla_ganador"
    android:background="@drawable/earth">

    <TextView
        android:text="FIN DE LA VERSIÓN DE PRUEBA"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/mensaje"
        android:textStyle="normal|bold|italic"
        android:textSize="30sp"
        android:textAlignment="center"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:lineSpacingExtra="14sp"
        android:textColor="@android:color/background_light" />

    <Button
        android:text="salir"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="155dp"
        android:id="@+id/salir"
        android:onClick="salir" />

    <Button
        android:text="menú"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/salir"
        android:layout_alignStart="@+id/salir"
        android:layout_marginTop="17dp"
        android:id="@+id/menu"
        android:onClick="menu" />

</RelativeLayout>
